function get_achievement(x,y,z){
  let sum = x + y + z;
  // ↑ｘ、ｙ、ｚの合計をsumに定義付け
  if(sum >= 250){
    // ↑sumが250以上だったら
  return "A";
  // Aを返す
}
}
console.log(get_achievement(60,200,40));
// ｘ、ｙ、ｚに入れる数字
